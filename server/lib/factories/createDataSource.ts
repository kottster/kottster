import { DataSource } from "@kottster/common";

/**
 * Create a data source
 * @param dataSource The data source to create
 * @returns The created data source
 */
export function createDataSource(dataSource: DataSource): DataSource {
  return dataSource;
}
